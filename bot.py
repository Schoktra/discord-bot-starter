from core import *

# define the bot and its prefix character(s)
bot = commands.Bot(command_prefix=">")
bot.load_extension('core')

# run the bot with your token
bird.run("place your token here")
